<assembly xmlns="http://maven.apache.org/ASSEMBLY/2.1.0"
          xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
          xsi:schemaLocation="http://maven.apache.org/ASSEMBLY/2.1.0
                              http://maven.apache.org/xsd/assembly-2.1.0.xsd">
    <id>dist</id>
    <formats>
        <format>zip</format>
        <format>tar.gz</format>
    </formats>
    <includeBaseDirectory>true</includeBaseDirectory>
    <baseDirectory>piknik-${project.version}</baseDirectory>

    <fileSets>
        <!-- Include the main JAR -->
        <fileSet>
            <directory>${project.build.directory}</directory>
            <outputDirectory>/</outputDirectory>
            <includes>
                <include>piknik-*-jar-with-dependencies.jar</include>
            </includes>
            <fileMode>0755</fileMode>
        </fileSet>

        <!-- Include external config directory -->
        <fileSet>
            <directory>${project.build.directory}/config</directory>
            <outputDirectory>/config</outputDirectory>
            <fileMode>0644</fileMode>
        </fileSet>

        <!-- Include external resources directory -->
        <fileSet>
            <directory>${project.build.directory}/resources</directory>
            <outputDirectory>/resources</outputDirectory>
            <fileMode>0644</fileMode>
        </fileSet>

        <!-- Include startup scripts -->
        <fileSet>
            <directory>${project.basedir}/src/main/scripts</directory>
            <outputDirectory>/</outputDirectory>
            <includes>
                <include>start.sh</include>
                <include>start.bat</include>
                <include>start-debug.sh</include>
                <include>start-debug.bat</include>
            </includes>
            <fileMode>0755</fileMode>
        </fileSet>

        <!-- Create empty logs directory -->
        <fileSet>
            <directory>${project.basedir}/src/main/scripts</directory>
            <outputDirectory>/logs</outputDirectory>
            <excludes>
                <exclude>**/*</exclude>
            </excludes>
            <fileMode>0755</fileMode>
        </fileSet>
    </fileSets>

    <files>
        <!-- Include README -->
        <file>
            <source>${project.basedir}/README.md</source>
            <outputDirectory>/</outputDirectory>
            <fileMode>0644</fileMode>
        </file>
    </files>
</assembly>
